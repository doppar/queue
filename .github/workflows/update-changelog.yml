name: Update Changelog

on:
  release:
    types: [released]

permissions: {}

jobs:
  update:
    permissions:
      contents: write
    uses: doppar/doppar/.github/workflows/update-changelog.yml@main